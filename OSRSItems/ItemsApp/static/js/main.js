$(document).ready(function() {
    let panel = $('.item_panel');

    if (panel != null) {
        // Fade out then in the panel
        panel.fadeOut(0).fadeIn(500);
    }
});

// Path: AppBuilder9000\ItemsApp\static\js\home.js